package utils;/*This class is to delete the first 2 lines of metamap output xml files, so that these files could be * Executed by xpath  * */import java.io.BufferedReader;import java.io.BufferedWriter;import java.io.File;import java.io.FileReader;import java.io.FileWriter;import java.util.ArrayList;public class XmlBuilder {		public static void main(String[] args) throws Exception{		if(args[0].equals("-help")){			showHelp();		}		else{		ArrayList<File> xmlfiles = FileFinder				.GetAllFiles(args[0], ".xml", true);		for (File f:xmlfiles){			addXml(f);					}		System.out.print("done!");		}	}	  private static void showHelp() {		  System.out.println("XmlBuilder: delete the first 2 lines of metamap output xml files, so that these files could be Executed by xpath ");			System.out.println("Usage:");			System.out.println("XmlBuilder [MetamapXmlsFolder] ");			System.out.println("XmlBuilder -h, --help \t show this help and exit");			}	public static void addXml(File f) throws Exception{		  		  ArrayList<String> list = new ArrayList<String>();		  BufferedReader reader =  new BufferedReader(new FileReader(f));		  String temp;		  while ((temp = reader.readLine()) != null)               list.add(temp);		  reader.close();		  //		  FileWriter fstream = new FileWriter(f);//		  BufferedWriter out = new BufferedWriter(fstream);		  BufferedWriter out = new BufferedWriter(new FileWriter(f));          for (int i = 2; i < list.size(); i++)        	  out.write(list.get(i) + "\r\n");          out.close();	  }}